ln(x^2*y^2)/2 -> ln(x*y)

ln(x^2*y^4)/2
____________________________________________________________________
abs(x=y) -> abs(x)=abs(y)
____________________________________________________________________

sqrt(sin(x)^2) -> abs(sin(x))

____________________________________________________________________
integral(d(sin(x)*inv(y))) -cant reverse
____________________________________________________________________

integral of (2*x+5)*(x^2+5*x)^7  
the problem is that it factors out the x which seperates the powers and thus u sub not possible

____________________________________________________________________
∫(2^x/(𝑒^(x+1))·∂(x)) -causes crash

caused by

𝑒^(x+1+1/ln(2))

____________________________________________________________________

∂(∫(2^x/(3^x)·∂(x)))
 unsimplified
 
int(d(e^x)) does not fully simplify

int((x+1)^2*x*d(x)) does not fully simplify

∂(ln(x)^-2/x)/∂(x)
 
____________________________________________________________________

____________________________________________________________________
